local TweenService = game:GetService("TweenService")

-- Create a ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Create a TextButton
local textButton = Instance.new("TextButton")
textButton.Size = UDim2.new(0, 200, 0, 50)  -- Button size
textButton.Position = UDim2.new(0.5, -100, 0.5, -25)  -- Button position (centered)
textButton.Text = "Click me!"
textButton.Parent = screenGui

-- Create a Sound object
local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://5129686200"  -- Replace with the actual sound ID
sound.Parent = game.Workspace

-- Play sound and release sphere when button is clicked
textButton.MouseButton1Click:Connect(function()
    sound:Play()  -- Play the sound when button is clicked
    
    -- Wait for 2 seconds before creating the sphere
    wait(2)
    
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

    -- Create the sphere
    local sphere = Instance.new("Part")
    sphere.Name = "ExpandingSphere"  -- Name it for easier identification
    sphere.Shape = Enum.PartType.Ball
    sphere.Size = Vector3.new(5, 5, 5)  -- Initial size of the sphere
    sphere.Position = humanoidRootPart.Position
    sphere.Anchored = true  -- Make sure it's anchored so it doesn't fall
    sphere.Material = Enum.Material.Forcefield
    sphere.Color = Color3.fromRGB(0, 0, 0)  -- Black color
    sphere.Parent = workspace

    -- Create a Tween for expanding the sphere
    local tweenInfo = TweenInfo.new(
        10,  -- Time to expand
        Enum.EasingStyle.Linear,  -- Easing style
        Enum.EasingDirection.InOut  -- Easing direction
    )
    local goal = {Size = Vector3.new(20, 20, 20)}  -- Final size of the sphere
    local tween = TweenService:Create(sphere, tweenInfo, goal)
    
    -- Play the tween to expand the sphere
    tween:Play()

    -- Wait for the tween to complete
    tween.Completed:Wait()

    -- Wait for an additional 5 seconds before deleting the sphere
    wait(5)
    
    -- Remove the sphere
    sphere:Destroy()
end)
